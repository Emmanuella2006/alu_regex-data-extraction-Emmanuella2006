# alu_regex-data-extraction-Emmanuella2006
A Python project that uses regular expressions to detect and validate real-world data (emails, URLs, phone numbers, currency) while rejecting unsafe or malformed input.
